      <nav class="header-nav">
        <ul class="header-nav-list">
          {% for navItem in nav.navItems %}
            <li class="header-nav-item {{ 'current' if navItem.slug in page.url }}">
              <a href="/{{ navItem.slug }}/">
                {{ navItem.title }}
              </a>
            </li>
          {% endfor %}
        </ul>
      </nav>