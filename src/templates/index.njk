---
layout: layouts/home.njk
title: Home
slug: home
tags: top
order: 4
cover:
    src: ./src/images/home_cover_square.png
    alt: Screenshot of charlesrobertson.io home page
    cardAlt: Screenshot of charlesrobertson.io home page
description: Charles Robertson is web designer and developer working to build a better web.
stylesheets: [facehead.css]
---
<h1 class="visually-hidden">Charles Robertson Code and Design</h1>

<section class="home-section home-intro">
    <p class="home-intro-desc site-container-padded">I am a <strong>designer</strong> and <strong>developer</strong> who is really into the web. I like reading about it, talking about it, and making things on it.</p>
    <div class="home-section-link-wrap">
        <a href="/about" class="home-section-link">About</a>
    </div>
</section>
<section class="home-section home-section-work home-postcard-selection">
    <div class="home-section-inner">
        <h2 class="site-container-padded">Latest Work</h2>
        <div class="home-postcards">
            {%- set postlist = (collections.work | sortByOrder(true)) %}
            {%- set post = postlist[0] %}
            {%- include "postcard.njk" %}
            {%- set post = postlist[1] %}
            {%- include "postcard.njk" %}
            {%- set post = postlist[2] %}
            {%- include "postcard.njk" %}
            <div></div>
        </div>
    </div>
    <div class="home-section-link-wrap">
        <a href="/work" class="home-section-link">Work</a>
    </div>
</section>
<section class="home-section home-section-lab home-postcard-selection">
    <div class="home-section-inner">
        <h2 class="site-container-padded">Latest from the Lab</h2>
        <div class="home-postcards">
            {%- set postlist = (collections.lab | sortByOrder(true)) %}
            {%- set post = postlist[0] %}
            {%- include "postcard.njk" %}
            {%- set post = postlist[1] %}
            {%- include "postcard.njk" %}
            {%- set post = postlist[2] %}
            {%- include "postcard.njk" %}
            <div></div>
        </div>
    </div>
    <div class="home-section-link-wrap">
        <a href="/lab" class="home-section-link">Lab</a>
    </div>
</section>